// Write your code here
import './index.css'

const AppItem = ({details}) => {
  const {appName, imageUrl} = details

  return (
    <li>
      <div className="appitem-continer">
        <img src={imageUrl} alt="appName" className="app-img" />
        <p>{appName}</p>
      </div>
    </li>
  )
}

export default AppItem
